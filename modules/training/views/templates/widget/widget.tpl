{block name='training'}
    <div class="training">
        <h4>{l s='Tsl training title' mod='training'}</h4>
        <p>
            {l s='The parameter from db tab : %s' sprintf=[$TrainingParameter] mod='training'}
        </p>
    </div>
{/block}
